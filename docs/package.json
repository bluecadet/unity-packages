{
  "name": "unity-packages-docs",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation site tooling for bluecadet/unity-packages. Not a Unity package.",
  "type": "module",
  "scripts": {
    "dev": "docs dev --config docs.config.yaml",
    "build": "docs build --config docs.config.yaml --out dist"
  },
  "dependencies": {
    "@bluecadet/docs": "^1.0.0"
  }
}
