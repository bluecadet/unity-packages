# Hap Video Test Fixtures

This directory contains test fixture files for the native Hap plugin.
All files are 640×360, 30 fps.

The `scripts/generate_*.py` / `generate_fixtures.sh` generators referenced
below, and the `*_frame0.png` visual stills, are not vendored here -- the
fixtures are committed binaries, and regenerating one means fetching the
generator from the upstream core repository noted in `Native~/README.md`.

## Auto-generated fixtures

The following fixtures are generated by `scripts/generate_fixtures.sh`:

| File | Variant | Chunks | Notes |
|------|---------|--------|-------|
| `hap1.mov` | Hap (BC1/DXT1) | 1 | Snappy compressor, ~2 s |
| `hap1_chunked.mov` | Hap (BC1/DXT1) | 4 | Complex/Snappy, 4 chunks, ~2 s |
| `hap1_audio.mov` | Hap (BC1/DXT1) | 1 | With PCM audio track, ~2 s |
| `hap1_frame0.png` | — | — | Golden reference frame 0 |
| `hap1_golden.bin` | — | — | Raw decoded BC1 frame 0 (115200 bytes) |
| `hap5.mov` | Hap Alpha (BC3/DXT5) | 1 | Snappy compressor, ~2 s |
| `hap5_chunked.mov` | Hap Alpha (BC3/DXT5) | 4 | Complex/Snappy, 4 chunks, ~2 s |
| `hap5_frame0.png` | — | — | Golden reference frame 0 |
| `hapy.mov` | Hap Q (YCoCg-DXT5) | 1 | Snappy compressor, ~2 s |
| `hapy_chunked.mov` | Hap Q (YCoCg-DXT5) | 4 | Complex/Snappy, 4 chunks, ~2 s |
| `hapy_frame0.png` | — | — | Golden reference frame 0 |

Generation requires ffmpeg built with `--enable-libsnappy` for Hap encoding
support (e.g. `homebrew-ffmpeg/ffmpeg/ffmpeg` or `ffmpeg-full`). Standard
Homebrew ffmpeg does not include snappy; use the Vidvox hap-ffmpeg fork or
build from source:

```bash
brew install snappy
git clone https://github.com/FFmpeg/FFmpeg.git
cd FFmpeg
./configure --enable-libsnappy --enable-libx264 --enable-gpl
make -j$(sysctl -n hw.logicalcpu)
```

To regenerate:

```bash
./scripts/generate_fixtures.sh tests/fixtures
```

## Hap7 fixture (BC7 / BPTC UNORM)

File: `hap7.mov` — generated by `scripts/generate_hap7_fixture.py`.

The Hap7 variant cannot be encoded by ffmpeg. The Vidvox AVF Batch Exporter
(macOS only) is the canonical encoder, but it is a GUI tool and not
reproducible in CI. Instead, this repo ships a Python generator that writes
a structurally valid MOV with a `Hap7` `stsd` entry and a real Hap7 frame
(None-compressed BC7 block bytes). This is sufficient to validate the
highest-risk demux path: minimp4 does not recognize `Hap7`, so dimensions
must come from the hand-parsed `stsd` fallback. The committed
`hap7.mov` is produced by this script.

The BC7 block bytes are a fixed pattern (mode 6, opaque), valid BC7 blocks
but not meaningful video content. The demuxer and decoder treat them as
opaque pass-through data — exactly what this fixture validates.

To regenerate:

```bash
python3 scripts/generate_hap7_fixture.py
```

### Encoding a real Hap7 clip with the Vidvox AVF Batch Exporter

If a content-meaningful Hap7 fixture is needed (e.g. to validate visual
output), use the AVF Batch Exporter (macOS):

1. **Source material**: Create a 640×360, 30 fps, ~2 second video clip in
   any format QuickTime Player can open (e.g., ProRes, H.264).
2. **Vidvox AVF Batch Exporter**
   (https://vidvox.net/avf_batch_exporter.html):
   - Drag the source clip into the batch list.
   - **Codec**: `Hap R`, **Quality**: `Default` (BC7 UNORM),
     **Compressor**: `Snappy`, **Chunks**: `1`.
   - Export.
3. **Verify** the FourCC:

   ```bash
   ffprobe -v 0 -show_streams hap7.mov | grep codec_tag
   # expected: codec_tag_string=Hap7
   ```

## HapM fixture (Hap Q Alpha / YCoCg DXT5 + A_RGTC1 dual texture)

File: `hapm.mov` — generated by `scripts/generate_hapm_fixture.py`, plus
golden references `hapm_golden_tex0.bin` (color, YCoCg DXT5) and
`hapm_golden_tex1.bin` (alpha, A_RGTC1) for frame 0.

HapM cannot be encoded by ffmpeg either, and unlike Hap7 (opaque
pass-through single-texture data is enough to validate its demux path),
HapM's highest-risk path is specifically decoding *two* independent
textures out of one frame -- the exact regression the reference Unity
plugin shipped, which decoded only texture 0 and silently dropped HapM's
alpha. The Python generator writes a real Multi-Image (`0x0D`) section
wrapping a YCoCg DXT5 section and an A_RGTC1 section, both None-compressed
(no python-snappy in this environment; the decoder's None path is fully
supported so nothing is lost for the demux/section-walking logic under
test). Texture bytes are deterministic pseudo-random data (fixed,
distinct seeds per texture) so an aliased-buffer regression would show up
as a byte mismatch rather than an accidental match.

To regenerate:

```bash
python3 scripts/generate_hapm_fixture.py
```

### Encoding a real HapM clip with the Vidvox AVF Batch Exporter

If a content-meaningful HapM fixture is needed (e.g. to validate visual
output), use the AVF Batch Exporter (macOS):

1. **Source material**: Create a 640×360, 30 fps, ~2 second clip **with a
   non-trivial alpha channel** (e.g. ProRes 4444 with varying
   transparency) -- a fully-opaque source would not exercise the
   dropped-alpha regression described above.
2. **Vidvox AVF Batch Exporter**
   (https://vidvox.net/avf_batch_exporter.html):
   - Drag the source clip into the batch list.
   - **Codec**: `Hap Q Alpha`, **Compressor**: `Snappy`, **Chunks**: `1`.
   - Export.
3. **Verify** the FourCC:

   ```bash
   ffprobe -v 0 -show_streams hapm.mov | grep codec_tag
   # expected: codec_tag_string=HapM
   ```

A chunked HapM (`hapm_chunked.mov`, **Chunks**: `4`) is a conditional
extra: per-texture chunk counts may differ in HapM, and a real chunked
file would settle that otherwise-untested path. Place it alongside if the
exporter produces one.

## Golden-frame regression tests

Byte-exact golden-frame tests live in `src/core/hap_decode.zig` and run via
`zig build test`. Three layers:

1. **Per-codec fixture decode** (`decodeTexture per-codec fixtures decode to
   the expected format and byte count`): demux each real `.mov` fixture,
   decode frame 0, and assert the texture format and decoded byte count for
   640×360 at that format's block size. These exercise the Snappy
   decompress path the synthetic frame tests don't cover.

2. **Chunked-vs-unchunked equivalence** (`decodeTexture chunked fixtures
   decode byte-identical to their unchunked counterparts`): chunking is
   transport, so a `*_chunked.mov` must decode to exactly the bytes its
   unchunked counterpart does.

3. **Golden compares**: `decodeTexture Hap1 fixture frame0 matches the
   committed golden texture` pins `hap1.mov` against `hap1_golden.bin`, and
   `decodeTexture Hap Q Alpha fixture frame0 decodes both textures against
   their goldens` demuxes `hapm.mov`, decodes both textures by index, and
   asserts their formats and byte-exact content against
   `hapm_golden_tex0.bin` / `hapm_golden_tex1.bin`.

The `*_frame0.png` stills alongside each fixture (extracted by
`generate_fixtures.sh`) are a visual reference, not a test assertion.

## Notes

- Neither HapM nor Hap7 (HapR) can be encoded by ffmpeg. Both `hapm.mov`
  and `hap7.mov` are committed fixtures generated by their respective
  Python scripts (`generate_hapm_fixture.py` / `generate_hap7_fixture.py`),
  not the ffmpeg script. A content-meaningful capture from the Vidvox AVF
  Batch Exporter (macOS only) is an optional follow-up, placed manually
  when available -- see each fixture's section above.
- Golden binary references (`*_golden.bin` / `*_golden_tex{0,1}.bin`) are
  the raw decoded texture data (BC blocks) of frame 0, byte-identical to
  what the decoder produces. The ffmpeg-generated fixtures' golden files
  are regenerated by running the decode test on the corresponding
  fixture; the HapM/Hap7 golden files are emitted directly by their
  generator scripts (the source of truth, since decode is a pass-through
  None-compressor copy for those fixtures).